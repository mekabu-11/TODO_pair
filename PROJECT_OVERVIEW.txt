# 📋 Food Map App (フードマップアプリ) プロジェクト概要

## 🎯 アプリ概要

お気に入りの飲食店を地図上にピン留めし、**写真やレビューで記録できるパーソナルグルメマップWebアプリ**です。

---

## ✨ 主な機能

| 機能 | 説明 |
|-----|------|
| 🗺️ **マップ管理** | Google Mapsベースの地図でお店を視覚的に管理 |
| ⭐ **レビュー** | 星評価（0〜5）と感想、写真を記録 |
| 📸 **写真インポート** | 写真のEXIF位置情報からピンを自動作成（HEIC対応 / 施設名自動取得） |
| 🔍 **検索** | 地名・住所でマップを移動 |
| 📋 **一覧表示** | 距離順ソート＆フィルター |
| 🔐 **認証** | ログイン機能、管理者によるユーザー管理 |

---

## 🛠️ 技術スタック

| レイヤー | 技術 |
|---------|------|
| **フロントエンド** | React 18, Vite 5, React Router 6 |
| **地図** | Google Maps API, @vis.gl/react-google-maps |
| **写真EXIF** | exif-js |
| **バックエンド** | Laravel 11, PHP 8.2+ |
| **データベース** | MySQL 8.0 |
| **Webサーバー** | Nginx (開発), Caddy (本番) |
| **インフラ** | Docker, Docker Compose |

---

## 🏗️ アーキテクチャ

```
ブラウザ (React Vite)
       │
       ▼ Axios HTTP
┌─────────────────────────────────────┐
│       Docker コンテナ群              │
│                                     │
│  開発: Vite:5173 → Nginx:8000 → Laravel → MySQL  │
│  本番: Caddy:443 → Laravel → MySQL               │
└─────────────────────────────────────┘
```

---

## 📁 ディレクトリ構成

| ディレクトリ | 説明 |
|------------|------|
| `frontend/` | React (Vite) フロントエンド |
| `frontend/src/pages/` | 画面コンポーネント（9ファイル） |
| `backend/` | Laravel バックエンド |
| `backend/app/Http/Controllers/` | APIコントローラー |
| `backend/app/Models/` | Eloquent モデル（Shop, Review, User, Photo） |
| `docker/` | Docker設定（php, nginx, caddy） |
| `docs/` | ドキュメント（設計書、デプロイ手順など） |

---

## 📱 画面構成

| パス | 画面 | 説明 |
|-----|------|------|
| `/` | MapPage | 地図画面（メイン） |
| `/list` | ShopListPage | 一覧＆フィルター |
| `/shop/:id` | ShopDetailPage | 詳細＆レビュー一覧 |
| `/add-shop` | AddShopPage | お店追加＆初回レビュー |
| `/shop/:id/review` | AddReviewPage | レビュー投稿 |
| `/review/:id/edit` | EditReviewPage | レビュー編集 |
| `/import` | PhotoImportPage | 写真インポート |
| `/login` | LoginPage | ログイン |
| `/users` | UserManagementPage | ユーザー管理（管理者専用） |

---

## 🔌 API エンドポイント

### 認証
| メソッド | URL | 説明 |
|---------|-----|------|
| POST | `/api/login` | ログイン |
| POST | `/api/logout` | ログアウト |
| GET | `/api/user` | ユーザー情報取得 |

### お店 (Shops)
| メソッド | URL | 説明 |
|---------|-----|------|
| GET | `/api/shops` | 一覧取得 |
| POST | `/api/shops` | 新規登録 |
| GET | `/api/shops/{id}` | 詳細取得 |
| PUT | `/api/shops/{id}` | 更新 |
| DELETE | `/api/shops/{id}` | 削除 |

### レビュー (Reviews)
| メソッド | URL | 説明 |
|---------|-----|------|
| POST | `/api/shops/{id}/reviews` | 投稿 |
| PUT | `/api/reviews/{id}` | 更新 |
| DELETE | `/api/reviews/{id}` | 削除 |

### ユーザー管理（管理者専用）
| メソッド | URL | 説明 |
|---------|-----|------|
| GET | `/api/users` | 一覧 |
| POST | `/api/users` | 作成 |
| DELETE | `/api/users/{id}` | 削除 |

---

## 🗄️ データベース設計

### ER図
```
┌────────────────────┐       ┌────────────────────┐
│       shops        │       │      reviews       │
├────────────────────┤       ├────────────────────┤
│ id (PK)           │───┐   │ id (PK)           │
│ name              │   │   │ shop_id (FK)      │◀──┘
│ latitude          │   └──▶│ rating (0-5)      │
│ longitude         │       │ content           │
│ category          │       │ photo_url         │
│ google_place_id   │       │ created_at        │
│ notes             │       │ updated_at        │
│ created_at        │       └────────────────────┘
│ updated_at        │
└────────────────────┘
```

### カテゴリ一覧
| キー | 表示名 | アイコン |
|-----|-------|---------| 
| restaurant | レストラン | 🍽️ |
| cafe | カフェ | ☕ |
| ramen | ラーメン | 🍜 |
| sushi | 寿司 | 🍣 |
| izakaya | 居酒屋 | 🍺 |
| fastfood | ファストフード | 🍔 |
| convenience | コンビニ | 🏪 |
| supermarket | スーパー | 🛒 |
| bakery | パン屋 | 🥐 |
| sweets | スイーツ | 🍰 |
| other | その他 | 📍 |

---

## 🚀 開発環境セットアップ

### 前提条件
- Docker & Docker Compose
- Google Maps API キー
- Google Cloud Console で Map ID を作成

### 起動手順
```bash
# 1. 環境変数を設定
cat > .env << 'EOF'
GOOGLE_MAPS_API_KEY=your-api-key
GOOGLE_MAP_ID=your-map-id
EOF

# 2. バックエンド環境変数をコピー
cp backend/.env.example backend/.env

# 3. Docker起動
docker compose up -d

# 4. DBマイグレーション
docker compose exec app php artisan migrate

# 5. ストレージリンク作成
docker compose exec app php artisan storage:link
```

### アクセスURL
| サービス | URL |
|---------|-----|
| フロントエンド | http://localhost:5173 |
| API | http://localhost:8000/api |

---

## 📚 関連ドキュメント

- [設計書（ARCHITECTURE.md）](./ARCHITECTURE.md) - 画面設計、API仕様、DB設計を図解付きで解説
- [バックエンド解説（BACKEND_GUIDE.md）](./BACKEND_GUIDE.md) - Railsエンジニア向けLaravel解説
- [フロントエンド解説（FRONTEND_GUIDE.md）](./FRONTEND_GUIDE.md) - バックエンドエンジニア向けReact解説
- [EC2デプロイ手順（EC2_DEPLOYMENT.md）](./EC2_DEPLOYMENT.md) - AWS EC2デプロイ手順
